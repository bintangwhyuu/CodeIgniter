pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                git 'https://github.com/bintangwhyuu/CodeIgniter.git'
            }
        }

        stage('Build') {
            steps {
                echo 'Build jalan...'
            }
        }
    }
}
